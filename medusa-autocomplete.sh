# nevermind the -W flag, it's obtrusive
#
# commands="
#   ansible-galaxy
#   ansible-playbook
#   ansible-vault
#   decrypt-file
#   encrypt-file
#   encrypt-string
#   go
# "
#
# complete -W "${commands}" -o bashdefault -o default 'medusa'

complete -W "${commands}" -o bashdefault -o default 'medusa'